FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json package-lock.json ./
RUN npm install          # install ALL deps including nodemon

# Source is mounted as a volume at runtime — no COPY needed here

EXPOSE 5000

CMD ["npm", "run", "dev"]
